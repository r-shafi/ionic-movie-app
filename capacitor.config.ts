import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shafi.openmovietracker',
  appName: 'Open Movie Tracker',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
